name: "energy_analytics"
version: "1.0.0"
config-version: 2

profile: "energy_analytics"

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  energy_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
